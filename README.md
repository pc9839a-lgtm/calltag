# 콜태그 (CallTag)

전화가 오면 고객의 현재 영업 단계와 최근 메모를 먼저 보여주고, 통화가 끝나면 발신·수신·부재중·거절 통화를 다음 행동까지 정리하는 페이지로 업무 서비스입니다.

## 제품 한 문장

> 전화 전에는 고객 상황을 보여주고, 전화 후에는 놓친 통화와 다음 행동을 남긴다.

## 페이지로 서비스 관계

콜태그와 콜링크는 서로 다른 앱이지만 같은 페이지로 업무 서비스다.

- `페이지로 콜링크`: 통화 후 문자 작성·발송·템플릿
- `페이지로 콜태그`: 고객 영업 단계·상담 이력·캘린더·후속업무
- `페이지로 비즈니스`: 공통 계정·고객·통화·결제·웹 관리 체계

앱과 도메인이 달라도 같은 페이지로 계정과 고객 데이터를 사용하도록 개발한다.

## 권장 웹 도메인

- 콜태그 웹: `https://calltag.pagero.kr`
- 향후 통합 포털: `https://app.pagero.kr`
- 공통 API: `https://api.pagero.kr`

초기 공개 주소는 `calltag.pagero.kr`을 사용한다.

## 현재 단계

- Android MVP 기능 개발
- 통화 종료 후 고객 분류·상담 결과 저장
- 고객 상세·메모·상태 변경·활동 이력
- 월간 캘린더와 날짜별 일정 관리
- 일정 추가·변경·완료·다시 열기·삭제
- 고객 추가 시 최근 통화 목록에서 번호·이름 불러오기
- 콜링크와 동일한 로그인·회원가입·이메일 인증·비밀번호 재설정
- 회원가입 필수 개인정보 수집·이용 및 이용약관 동의
- 계정정보·개인정보처리방침·로그아웃·2단계 회원탈퇴
- 통화 후 중복 저장·버튼 연타·권한 누락·서비스 재시작 안정화
- 사용자별 영업 단계 추가·이름 변경·순서 변경·삭제
- 고객 목록에서 영업 단계 즉시 변경
- 고정 상태 필터를 선택형 상태 필터로 전환
- VIP 기본 상태와 고객 유입경로 입력 제거
- 캘린더 선택 배경·일정 표시 잘림 안정화
- 수신 전화 시 고객명·영업 단계·최근 메모 알림
- 발신·수신·부재중·거절 통화 미처리 대기열
- 발신 0초 통화를 `연결 안 됨`으로 분류
- 홈에서 미처리 통화 `전화 / 정리 / 완료` 처리
- 통화 정리 저장 시 미처리 목록 자동 제거
- 콜링크 연동형 웹 서비스 구조 문서화
- 패키지명: `kr.pagero.calltag`
- 최소 Android: API 26
- 수신 고객정보 표시: Android 10 이상 권장
- 개발 브랜치: `agent/calltag-foundation`
- 최근 개발 버전: `v0.10.0`

## 통화 흐름

### 전화가 올 때

홈의 `수신 고객정보 표시 켜기`에서 연락처 권한과 Android 수신정보 앱 역할을 설정한다.

등록된 고객 전화에는 다음 정보가 상단 알림으로 표시된다.

- 고객명
- 현재 사용자 지정 영업 단계
- 최근 통화 메모
- 고객 상세 바로가기

### 전화가 끝난 뒤

수신·발신·부재중·거절 통화는 홈의 `처리할 통화`에 남는다.

- `전화`: 해당 번호로 전화
- `정리`: 통화 결과·메모·다음 연락 입력
- `완료`: 별도 기록 없이 목록에서 제거

부재중·거절·연결되지 않은 발신 통화는 정리 화면을 강제로 열지 않고 알림과 홈 목록으로 관리한다.

## 영업 단계 기준

기본 단계는 아래와 같다.

1. 신규 문의
2. 요구 확인
3. 견적·자료 발송
4. 검토 중
5. 계약 예정
6. 계약 완료

이 단계는 사용자가 직접 추가·이름 변경·순서 변경·삭제할 수 있다.

고객 상태는 고객 등급이 아니라 현재 영업 진행 위치를 의미한다. `VIP`처럼 기준이 불명확한 고객 등급은 기본 상태로 사용하지 않는다.

고객 등록 시 유입경로를 묻지 않는다. 향후 시스템에서 자동 확인할 수 있는 경우에만 내부 데이터로 저장한다.

## 계정과 회원탈퇴

- 콜태그는 페이지로 콜링크와 같은 회원가입·로그인 서버를 사용한다.
- 회원가입 시 이름, 휴대폰번호, 이메일, 브랜드명, 업종을 수집한다.
- 개인정보 수집·이용과 서비스 이용약관에 각각 필수 동의해야 가입할 수 있다.
- 로그인 세션은 Android Keystore 기반 AES-GCM으로 암호화해 저장한다.
- 로그아웃하면 통화 감지는 중지하지만 휴대전화의 고객·일정 데이터는 유지한다.
- 회원탈퇴는 두 번 확인하며, 공유 페이지로 계정과 콜태그의 로컬 고객·상담·일정 데이터를 삭제한다.
- 같은 계정을 사용하는 콜링크도 탈퇴한 계정으로 다시 로그인할 수 없다.

## 핵심 문서

- `docs/PRODUCT_SPEC_KO.md` — 앱·웹 통합 제품 정의와 MVP 범위
- `docs/CRM_STAGE_CUSTOMIZATION_KO.md` — 사용자 정의 영업 단계와 고객 목록 UX 기준
- `docs/CALL_FLOW_MANAGEMENT_KO.md` — 수신 고객정보·발신·부재중·거절 통화 관리 기준
- `docs/WEB_SERVICE_SPEC_KO.md` — 콜태그 웹 화면·기능·도메인 기획
- `docs/CALLLINK_INTEGRATION_ARCHITECTURE_KO.md` — 콜링크·콜태그 공통 계정·데이터·API 구조
- `docs/ACCOUNT_PRIVACY_FLOW_KO.md` — 회원가입·개인정보 동의·로그아웃·회원탈퇴 기준
- `docs/CUSTOMER_CLASSIFICATION_KO.md` — 신규 번호와 기존 고객 판별 기준
- `docs/DATA_MODEL_KO.md` — 고객·상담건·후속업무 데이터 구조
- `docs/SCREEN_FLOW_KO.md` — 앱·웹·콜링크 연결 화면 흐름
- `docs/DESIGN_SYSTEM_KO.md` — 페이지로 콜링크 기반 UI·클릭·반응형 규칙
- `docs/UI_UX_PATCH_03_KO.md` — 고객·전체 탭 짤림 및 클릭 구조 패치 명세
- `docs/ROADMAP_KO.md` — Android·웹·콜링크 연동 개발 순서

## 핵심 원칙

1. 고객과 상담건을 분리한다.
2. 고객 상태는 고객 등급이 아니라 영업 진행 단계를 의미한다.
3. 영업 단계는 사용자 업무에 맞게 직접 편집할 수 있어야 한다.
4. 고객 상태는 고객 목록과 상세 화면에서 즉시 변경할 수 있어야 한다.
5. 휴대폰 연락처 등록 여부만으로 기존 고객을 판단하지 않는다.
6. 통화 전에는 고객 단계와 최근 메모를 바로 확인할 수 있어야 한다.
7. 수신·발신·부재중·거절 통화를 모두 관리한다.
8. 통화 후 처리는 10초 안에 끝나야 한다.
9. 일정은 Android 앱·웹 캘린더·고객 상세에서 같은 상태로 보여야 한다.
10. 상태 변경·일정·문자 발송은 하나의 고객 활동 기록에 남긴다.
11. 고객 추가의 기본 진입은 최근 통화 목록으로 한다.
12. 콜링크와 콜태그는 같은 계정·고객·통화 식별자를 사용한다.
13. 앱별로 고객 데이터를 중복 생성하지 않는다.
14. 클릭 가능한 요소와 읽기 전용 정보를 같은 모양으로 만들지 않는다.
15. 360dp 화면과 큰 글자 환경에서 좌우 잘림을 허용하지 않는다.
