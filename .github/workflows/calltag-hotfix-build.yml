name: CallTag 0.44.0 call-log memo regression

on:
  workflow_dispatch:
  push:
    branches:
      - agent/play-internal-v0430-run2
      - agent/calltag-v0440-calllog-memo
  pull_request:
    branches:
      - agent/play-internal-v0430-run2

permissions:
  contents: read
  actions: read

jobs:
  android-build:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify 0.44.0 call-log-only memo contracts
        shell: bash
        run: |
          set -euo pipefail
          grep -F 'versionCode 78' app/build.gradle
          grep -F "versionName '0.44.0'" app/build.gradle
          grep -F 'targetSdk 36' app/build.gradle
          grep -F 'android.permission.WRITE_CALL_LOG' app/src/main/AndroidManifest.xml
          grep -F 'CallLog.Calls.CACHED_NAME' app/src/main/java/kr/pagero/calltag/CallLogMemoSyncManager.java
          grep -F 'requestSyncForCall' app/src/main/java/kr/pagero/calltag/PostCallActivity.java
          grep -F 'requestSyncForCustomer' app/src/main/java/kr/pagero/calltag/CustomerQuickEditActivity.java
          grep -F 'addIfMissing(missing, Manifest.permission.WRITE_CALL_LOG)' app/src/main/java/kr/pagero/calltag/InitialPermissionActivity.java
          ! grep -F 'addIfMissing(missing, Manifest.permission.WRITE_CONTACTS)' app/src/main/java/kr/pagero/calltag/InitialPermissionActivity.java
          ! grep -F 'createAppRawContact' app/src/main/java/kr/pagero/calltag/ContactNameSyncManager.java
          ! grep -F 'TYPE_KEEP_TOGETHER' app/src/main/java/kr/pagero/calltag/ContactNameSyncManager.java
          ! grep -F 'StructuredName.DISPLAY_NAME' app/src/main/java/kr/pagero/calltag/ContactNameSyncManager.java
          grep -F 'setPendingIntentCreatorBackgroundActivityStartMode' app/src/main/java/kr/pagero/calltag/PostCallActivityLauncher.java
          grep -F 'MODE_BACKGROUND_ACTIVITY_START_ALLOW_ALWAYS' app/src/main/java/kr/pagero/calltag/PostCallActivityLauncher.java
          grep -F 'new_intent_visible' app/src/main/java/kr/pagero/calltag/PostCallActivity.java
          grep -F 'markPendingHandledSafely' app/src/main/java/kr/pagero/calltag/PostCallActivity.java
          grep -F 'postCallFieldsScroll' app/src/main/res/layout/activity_post_call.xml
          grep -F 'todayWindow()' app/src/main/java/kr/pagero/calltag/MainActivityCardInteractionFix.java
          grep -F 'count_mismatch' app/src/main/java/kr/pagero/calltag/MainActivityCardInteractionFix.java

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Assemble debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: calltag-v0.44.0-code78-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run 8 P0 regression tests on Android 16
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 36
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: gradle :app:connectedDebugAndroidTest --stacktrace

      - name: Upload instrumentation results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: calltag-v0.44.0-code78-instrumentation-results
          path: |
            app/build/reports/androidTests/connected/**
            app/build/outputs/androidTest-results/connected/**
          if-no-files-found: ignore

      - name: Restore verified Play upload key
        env:
          GH_TOKEN: ${{ github.token }}
          SECRET_UPLOAD_KEYSTORE_BASE64: ${{ secrets.CALLTAG_UPLOAD_KEYSTORE_BASE64 }}
          SECRET_UPLOAD_STORE_PASSWORD: ${{ secrets.CALLTAG_UPLOAD_STORE_PASSWORD }}
          SECRET_UPLOAD_KEY_ALIAS: ${{ secrets.CALLTAG_UPLOAD_KEY_ALIAS }}
          SECRET_UPLOAD_KEY_PASSWORD: ${{ secrets.CALLTAG_UPLOAD_KEY_PASSWORD }}
          ORIGINAL_UPLOAD_KEY_ARTIFACT_ID: '8922836146'
          EXPECTED_UPLOAD_KEY_SHA256: 'C3:4C:98:88:9B:0C:88:8A:BB:39:94:6C:80:16:96:C2:89:E2:82:6C:10:0F:41:7A:0B:CE:25:A3:92:C4:72:A7'
        shell: bash
        run: |
          set -euo pipefail
          KEYSTORE="$RUNNER_TEMP/calltag-upload.jks"
          configured=0
          for value in "$SECRET_UPLOAD_KEYSTORE_BASE64" "$SECRET_UPLOAD_STORE_PASSWORD" "$SECRET_UPLOAD_KEY_ALIAS" "$SECRET_UPLOAD_KEY_PASSWORD"
          do
            [ -n "$value" ] && configured=$((configured + 1))
          done

          if [ "$configured" -ne 0 ] && [ "$configured" -ne 4 ]; then
            echo '::error::Upload-key Secrets are only partially configured.'
            exit 1
          fi

          if [ "$configured" -eq 4 ]; then
            printf '%s' "$SECRET_UPLOAD_KEYSTORE_BASE64" | base64 --decode > "$KEYSTORE"
            STORE_PASSWORD="$SECRET_UPLOAD_STORE_PASSWORD"
            KEY_ALIAS="$SECRET_UPLOAD_KEY_ALIAS"
            KEY_PASSWORD="$SECRET_UPLOAD_KEY_PASSWORD"
          else
            mkdir -p "$RUNNER_TEMP/original-upload-key"
            gh api "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ORIGINAL_UPLOAD_KEY_ARTIFACT_ID}/zip" > "$RUNNER_TEMP/original-upload-key.zip"
            unzip -q "$RUNNER_TEMP/original-upload-key.zip" -d "$RUNNER_TEMP/original-upload-key"
            cp "$RUNNER_TEMP/original-upload-key/calltag-upload.jks" "$KEYSTORE"
            CREDENTIALS="$RUNNER_TEMP/original-upload-key/CALLTAG_UPLOAD_KEY_CREDENTIALS.txt"
            STORE_PASSWORD="$(grep '^CALLTAG_UPLOAD_STORE_PASSWORD=' "$CREDENTIALS" | cut -d= -f2-)"
            KEY_ALIAS="$(grep '^CALLTAG_UPLOAD_KEY_ALIAS=' "$CREDENTIALS" | cut -d= -f2-)"
            KEY_PASSWORD="$(grep '^CALLTAG_UPLOAD_KEY_PASSWORD=' "$CREDENTIALS" | cut -d= -f2-)"
          fi

          test -s "$KEYSTORE"
          echo "::add-mask::$STORE_PASSWORD"
          echo "::add-mask::$KEY_PASSWORD"
          chmod 600 "$KEYSTORE"
          keytool -list -v -keystore "$KEYSTORE" -storepass "$STORE_PASSWORD" -alias "$KEY_ALIAS" \
            | grep -E 'Alias name:|Owner:|SHA1:|SHA256:' > "$RUNNER_TEMP/upload-key-fingerprints.txt"
          grep -F "SHA256: $EXPECTED_UPLOAD_KEY_SHA256" "$RUNNER_TEMP/upload-key-fingerprints.txt"
          {
            echo "CALLTAG_UPLOAD_KEYSTORE_PATH=$KEYSTORE"
            echo "CALLTAG_UPLOAD_STORE_PASSWORD=$STORE_PASSWORD"
            echo "CALLTAG_UPLOAD_KEY_ALIAS=$KEY_ALIAS"
            echo "CALLTAG_UPLOAD_KEY_PASSWORD=$KEY_PASSWORD"
          } >> "$GITHUB_ENV"

      - name: Build signed Play release AAB
        run: gradle :app:bundleRelease --no-daemon --stacktrace

      - name: Verify and stage signed AAB
        shell: bash
        run: |
          set -euo pipefail
          AAB='app/build/outputs/bundle/release/app-release.aab'
          test -s "$AAB"
          jarsigner -verify -certs "$AAB"
          OUT="$RUNNER_TEMP/calltag-v0.44.0-play"
          mkdir -p "$OUT"
          cp "$AAB" "$OUT/CallTag-v0.44.0-code78.aab"
          sha256sum "$AAB" > "$OUT/CALLTAG_V0440_AAB_SHA256.txt"
          cp "$RUNNER_TEMP/upload-key-fingerprints.txt" "$OUT/CALLTAG_UPLOAD_KEY_FINGERPRINTS.txt"

      - name: Upload Play Console AAB
        uses: actions/upload-artifact@v4
        with:
          name: calltag-v0.44.0-code78-play-aab
          path: ${{ runner.temp }}/calltag-v0.44.0-play
          retention-days: 90
          if-no-files-found: error
