# 콜태그 개발 현황·로드맵

기준일: **2026-08-08**  
저장소: `pc9839a-lgtm/calltag`  
제품 브랜치: `agent/play-internal-v0430-run2`  
현재 앱 버전: **0.44.5**  
versionCode: **83**  
패키지명: `kr.pagero.calltag`

> 이 문서는 현재 구현 상태의 정본이다. 오래된 `0.3x~0.41.x`, Classic/Pro, 3일 무료체험, 추천 +5일, 시스템 CallLog 메모 쓰기, 연락처 이름 변조 방식은 현재 기준이 아니다.

## 1. 현재 제품 구조

콜태그는 Android 전화 기반 고객관리 앱이며 기능군은 다음과 같이 분리한다.

| 상품 | 월 가격 | 현재 제품 정의 |
|---|---:|---|
| 전화관리 | 1,900원 | 수신 고객 표시, 통화 후 고객관리, 상태·메모·일정·통계 |
| 문자자동화 | 990원 | 통화 후 자동문자, 템플릿, 그룹·단체문자, 발송 관리 |
| 페이지로 | 3,500원 | 모바일 랜딩페이지 제작·문의 수집 |
| 통합권 | 6,000원 | 전화관리 + 문자자동화 + 페이지로 전체 |

앱의 Google Play Billing Library 의존성은 존재하지만 **실제 Google Play 구매·구독·복원·만료 서버 검증은 아직 완료되지 않았다.** 결제 UI가 보여도 `결제 준비 중` 상태와 실제 구매 완료 상태를 혼동하지 않는다.

## 2. 무료체험·추천 정책

현재 확정 정책:

- 일반 신규가입: **통합권 7일 무료**
- 추천인 코드 입력: **+7일**
- 추천가입자: **통합권 총 14일 무료**
- 추천인 코드는 **회원가입 시 1회만 입력**
- 가입 이후 추천코드 재적용 경로 없음
- 무료기간은 서버 entitlement로 처리하며 결제수단 등록을 요구하지 않음
- 무료체험 종료 후 자동결제하지 않음
- 추천인과 파트너 프로그램은 분리

페이지로 웹의 기존 추천 정책과 콜태그 앱 추천 정책은 서버에서 분리한다.

## 3. code83까지 완료된 핵심 안정화

### 3.1 통화 감지·복구

- `CallProcessingLedger`: 최근 CallLog ID 다중 영속 dedupe
- `CallInteractionDeduper`: 동일 통화 interaction DB 중복 삽입 방지
- `PostCallRecoveryStore`: 통화 후 팝업 전달 실패·프로세스 재시작 복구
- `CallLogRepository.findRecent`: 재시작 후 최근 통화 읽기 복구
- `CallDisposition`: 수신/발신/부재중/거절/0초 발신 규칙 중앙화
- ROLE_CALL_SCREENING 상태 자동 추적
- 삼성/Pixel/OEM 진단 로그 추가

Android 16/API36 에뮬레이터 회귀검사는 통과했다. **삼성 실기기 전체 회귀를 CI가 대신한 것으로 표현하지 않는다.**

### 3.2 Play 정책 안전 구조

현재 Play용 구조에서는 다음을 금지한다.

- `WRITE_CALL_LOG`
- `CallLog.Calls.CACHED_NAME` 쓰기
- 시스템 기본 전화앱 최근통화 메모 변조
- 사용자 원본 연락처 이름 변경
- 새로운 RawContact 생성으로 발신자 이름 강제 덮어쓰기

즉 콜태그 메모는 **콜태그 DB/UI에서 관리**한다. 삼성/구글 기본 전화앱 최근기록에 임의 메모를 써넣지 않는다.

### 3.3 통화 종료 팝업

현재 UX 기준:

- 통화 종료 후 **작은 팝업 1개만 표시**
- 전체화면 팝업 금지
- 고객명 + 메모 + 저장/닫기 중심
- 전화 메타정보를 과도하게 노출하지 않음
- 배경 전체 강한 dim 금지
- 같은 통화 Activity 중복 재실행 금지
- 직접 Activity 표시 실패 시 fallback은 알림만 사용

`PostCallActivity`는 CallTag DB만 수정한다.

## 4. 홈·고객·일정·통계 현재 동작

### 홈

- `오늘 할 일`은 오늘 일정만 표시
- 홈 고객 카드 → 고객 빠른 수정/상세 진입
- 확인할 통화 카드에 최근 메모 표시
- 확인할 통화에서 `할 일 등록` → `HomeTaskEditorActivity`
- `할 일 등록`이 통화 후 메모 팝업으로 연결되지 않음
- 확인할 통화 `삭제` 제공
- 할 일 저장 후 홈 복귀 시 즉시 갱신

### 고객 추가 > 최근 통화

code83부터 실제 Android `CallLog`를 최신순으로 읽는다.

- 동일 번호 여러 통화를 임의 dedupe하지 않음
- 이미 콜태그에 등록된 고객 번호를 숨기지 않음
- 등록된 번호는 `등록됨`으로 표시
- 등록된 번호 선택 시 새 고객을 만들지 않고 기존 고객으로 이동
- 화면 복귀 시 최근통화를 다시 로드
- 최대 60건 표시

삼성/Pixel 기본 전화앱은 연속 통화를 시각적으로 묶을 수 있으므로 화면 모양은 다를 수 있지만, 콜태그는 raw CallLog row 기준으로 표시한다.

### 일정

- 일정 화면에는 최근통화 목록을 넣지 않음
- 월간 캘린더 + 선택일 일정 중심
- 일정 추가·변경·완료·다시 열기·삭제

### 통계

- 일별 추이 차트 터치 지원
- 터치한 날짜의 `통화 N건 · 페이지로 N명` 상세 수치 표시

## 5. 더보기·페이지로·정산

더보기 핵심 구성:

- `통화 후 자동문자`는 일반 메뉴에 묻지 않고 **검색창 아래 대형 독립 카드**로 표시
- 문자 문구·이미지
- 그룹·단체문자
- 발송 관리
- 고객 상태
- 일정 종류
- 계정 및 개인정보
- 데이터 보호·복구
- 이용권·결제
- 친구 초대·파트너
- 파트너 정산
- 페이지로 연동
- 페이지로 서비스 안내
- 백업 및 복원

페이지로 관련 화면은 반드시 분리한다.

- `페이지로 연동`: 계정 연결 상태·문의 확인 중심
- `페이지로 서비스 안내`: 페이지로가 무엇인지 설명

연결 화면에 `이렇게 사용하세요` 같은 서비스 설명을 다시 섞지 않는다.

정산 연결:

`https://calltag.pagero.kr/web/settlement`

앱의 파트너 정산 화면에서 위 정산페이지로 연결한다.

## 6. UI 디자인 기준

code83 기준으로 일반 화면은 다크 UI를 기본으로 한다.

- 배경 `#101113`
- 카드 `#1C1E22`
- 주요색 `#4389FF`
- 기본 텍스트 밝은색
- Android 기본 흰색 AlertDialog/DatePicker/TimePicker가 튀어나오지 않도록 CallTag Dialog Theme 적용
- 확인/등록/저장: 파란 primary 버튼
- 취소/닫기: 다크 secondary 버튼
- 상태바 시간·배터리·신호 아이콘은 어두운 배경에서 흰색으로 표시
- system bar inset은 한 경로만 적용
- `fitsSystemWindows`와 전역 inset을 중복 적용하지 않음

## 7. 페이지로 실시간 문의 연동

현재 구조:

```text
페이지로 문의 접수
→ 서버 저장
→ CallTag FCM 알림
→ 전화번호 기준 고객 생성/갱신
→ 문의 내용을 고객 상담이력에 반영
```

기존 원칙:

- 앱 종료·잠금 상태 알림 대응
- 같은 문의 반복 처리 방지
- FCM 실패가 페이지로 문의 접수 자체를 실패시키지 않음
- FCM payload에 불필요한 개인정보를 넣지 않음

## 8. 문자 자동화 현재 방향

핵심 유형:

- 수신
- 발신
- 부재중
- 후속

기능군:

- 템플릿 저장·수정·복제·삭제
- 그룹·단체문자
- 발송 제외
- 중복발송 방지
- 예약·후속
- 발송내역
- 회선/업무시간 고려

`통화 후 자동문자`는 제품 핵심 기능으로 더보기 상단에서 바로 진입 가능해야 한다.

## 9. 데이터 보호·복구

콜태그 고객 데이터는 앱 내부 DB를 기본 정본으로 사용한다.

- 앱 삭제·기기변경을 대비한 데이터 보호/복구 기능 유지
- 기존 고객·메모·일정·문자 데이터 초기화 금지
- 서버 복구본과 기기 데이터 삭제는 다른 작업으로 취급
- 로그인 세션/토큰을 일반 백업 데이터와 혼합하지 않음
- 원본 휴대폰 전체 연락처/문자함/통화녹음을 서버로 올리는 제품이 아님

## 10. 최신 릴리스 검증

현재 제품 릴리스:

- versionName: `0.44.5`
- versionCode: `83`
- targetSdk: `36`
- 제품 PR: `#64`
- 제품 병합 SHA: `ca8ccc8bf8422764abfb23d8776f5e755f50f270`

검증:

- 정적 release contract 검사 PASS
- Debug APK 빌드 PASS
- Android 16/API36 전화 CRM 회귀검사 PASS
- 기존 Play upload key 지문 검증 PASS
- signed AAB 생성 및 `jarsigner` 검증 PASS

code83 AAB SHA-256:

`1af9a6c21cef9e1e6f3e179f9c1bf59ca1f1bda85276e8f354ac00df2160e3c5`

## 11. 다음 우선순위

### P0 — 실제 결제

다음 큰 기능은 Google Play 결제 완성이다.

필수 범위:

1. Play Console 구독/상품 ID 확정
2. `BillingClient` 구매 플로우
3. 구매 acknowledgement
4. 서버 영수증/구독 검증
5. CALL / MESSAGE / PAGERO / ALL_IN_ONE entitlement 분리
6. 구매 복원
7. 해지·만료·grace period·account hold 처리
8. 웹 페이지로 결제와 앱 결제 중복 방지
9. 무료체험 7일/추천 14일 entitlement와 유료 entitlement 전환
10. 실제 Play License Tester E2E

### P1 — 실기기 통화 회귀

- 삼성 기본 전화
- Pixel/Google Phone
- 에이닷 사용 환경
- 앱 foreground/background/잠금
- 연속 통화
- 부재중·거절·0초 발신
- 통화 직후 작은 팝업 단일 표시
- 고객 추가 최근통화와 실제 CallLog 비교

### P1 — UI 전수점검

- 남은 OEM 기본 흰색 Dialog/Picker 제거
- 상태바 공백/아이콘 전 화면 확인
- 작은 화면/큰 글자/키보드 대응
- 메뉴 중복 제거

### P2 — 사용자용 전화 기록 경험

기본 전화앱 CallLog를 변조하는 대신 콜태그 내부에서:

- 최근통화 검색/필터
- 고객별 통화·메모·일정·페이지로 문의 통합 타임라인
- 통화 카드에서 빠른 메모 수정

## 12. 절대 회귀 금지

- 통화 후 전체화면 팝업으로 되돌리지 않음
- 작은 팝업을 두 번 띄우지 않음
- 홈 `할 일 등록`을 메모 저장 팝업에 연결하지 않음
- 일정 탭에 최근통화를 다시 넣지 않음
- 고객 추가 최근통화에서 등록 고객/중복 통화를 숨기지 않음
- `WRITE_CALL_LOG`를 다시 추가하지 않음
- 시스템 CallLog `CACHED_NAME`을 수정하지 않음
- 사용자 원본 연락처 이름을 덮어쓰지 않음
- 페이지로 연동과 서비스 안내를 한 화면으로 다시 합치지 않음
- 추천인 코드를 가입 이후 다시 입력하게 만들지 않음
- 3일/5일 무료체험 정책으로 되돌리지 않음
- Classic/Pro 요금제로 되돌리지 않음
