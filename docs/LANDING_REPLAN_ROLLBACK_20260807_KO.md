# 콜태그 랜딩 전면 재기획 원복 기준

기준일: 2026-08-07

## 원복 기준

- 원복 기준 커밋: `dea6926b55ad7dabf1bbfe7f10af35421803d870`
- 백업 브랜치: `backup/landing-before-replan-20260807-2304`
- 이 기준은 랜딩 전면 재기획 패치 직전의 `main` 상태다.

## 이번 패치 방식

기존 `web/index.html`은 직접 수정하지 않는다.

신규 기획안은 별도 파일로 추가한다.

- 신규 랜딩: `web/index-replan-20260807.html`
- 루트 전환: `web/_redirects`의 `/ /index-replan-20260807.html 200`

따라서 기존 랜딩 원본은 저장소에 그대로 남아 있다.

## 즉시 원복 방법

가장 단순한 원복은 `web/_redirects`에서 아래 한 줄만 제거하는 것이다.

```text
/ /index-replan-20260807.html 200
```

그러면 기존 `web/index.html`이 다시 루트 페이지로 노출된다.

전체 패치 이전 상태로 되돌려야 하면 `main`을 아래 백업 브랜치 기준으로 되돌린다.

```text
backup/landing-before-replan-20260807-2304
```

또는 기준 커밋:

```text
dea6926b55ad7dabf1bbfe7f10af35421803d870
```

## 이번 재기획 핵심

- 기존 디자인 톤 유지: 검정 배경, 대형 타이포, 파란 포인트, 폰 목업, 카드형 UI
- 실제 Android 0.44.2 제품 흐름 기준으로 카피 재작성
- 전화 수신 전 고객정보 표시를 첫 핵심 기능으로 승격
- 통화 종료 화면은 현재 구현대로 고객명·메모 중심으로 단순화
- 홈·오늘 할 일·고객·문자·통계 기능 반영
- 페이지로는 별도 서비스로 유지하되 문의 자동등록·실시간 알림 연결을 한 섹션에서 설명
- 메인 판매 상품은 콜태그+페이지로 통합 월 6,000원으로 정리
- 구버전 베타 후기, 7일 무료, 1,900원 메인 요금 노출 제거

## 관련 패치 커밋

- `8571837110b589fabd865b3b20ea9154bdf7a7fe` — 신규 랜딩 추가
- `b3ace48ec639545350aae9141b4de307a6934b01` — 루트 라우팅 전환
